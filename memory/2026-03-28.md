# Memory 2026-03-28

- Người dùng xác nhận phong cách trả lời: thuần Việt, ngắn gọn; không nêu bằng chứng/đường dẫn trong trả lời thường ngày; chỉ nêu vướng mắc khi thực sự gặp.
- Đã chuẩn hóa quy trình Facebook trong skill `facebook-content-ops`: bắt buộc SOP Viết -> Duyệt -> Đăng + Prepublish Gate để tránh lặp lỗi, giảm trùng nội dung.
- Quy tắc vận hành Facebook đã chốt: khi nội dung đã duyệt và chốt giờ đăng thì tự đăng, không hỏi lại; đăng xong phải báo kết quả và link bài.
- Đã lưu hồ sơ thương hiệu FUJI TH vào skill để dùng lâu dài: thông tin công ty, cụm từ khóa SEO, guideline giọng thương hiệu.
- Đã thêm hướng dẫn random writing style trong skill để giảm trùng lặp nội dung giữa các bài.
- Đã đăng bài website về chủ đề động cơ thang máy nhập khẩu; website WordPress của FUJI TH hoạt động bình thường qua REST.
- Đã fix luồng WordPress skill REST cho trường hợp site không ổn định route `/wp-json/*` bằng fallback `?rest_route=`; preflight/list đã hoạt động ổn định sau fix.
- Đã tạo và dùng category "Chia sẻ kinh nghiệm" trên website để đăng bài kèm ảnh theo yêu cầu.
- Đã cập nhật OpenClaw lên bản 2026.3.24 thành công.
- Đã bật Telegram exec approvals cho 2 ID: 8451930371 và 5742018098.
- Đã mở auto approval exec (`tools.exec.ask=off`) và nới quyền thực thi (`tools.exec.security=full`), đồng thời allow `bash`, `curl` theo yêu cầu.
- Đã hỗ trợ session ID 8451930371 bằng hướng dẫn credential path cho Facebook/WordPress và script chẩn đoán nhanh `scripts/agent_skill_doctor.sh`.
- Đã cấu hình bảo trì máy chủ theo yêu cầu: dọn rác hàng tuần + kiểm tra dung lượng mỗi 3 ngày.
- Người dùng duyệt áp dụng quy trình đa kênh rút gọn; đã thêm tài liệu `multichannel-lite` vào skill `facebook-content-ops` để giảm phình context và lỗi API khi chạy Facebook + Website.
- Đã tạo bài website chủ đề “thời điểm lắp đặt thang máy lý tưởng”, hẹn đăng 09:00 ngày 2026-03-29, chuyên mục “Chia sẻ kinh nghiệm”, có ảnh đại diện.
- Người dùng nhắc rõ yêu cầu SEO ảnh; đã cập nhật hướng dẫn WordPress: trước publish/schedule bắt buộc tối ưu tên file ảnh, alt text, title/caption/description và featured image.
- Đã lên lịch bài Facebook cho 08:30 ngày 2026-03-29 kèm ảnh theo nội dung bản rút gọn đã duyệt.
- Đã cài bộ thư viện xử lý Word/Excel qua pip (`python-docx`, `docx2txt`, `openpyxl`, `pandas`, `xlrd`, `xlsxwriter`, `pyxlsb`) bằng `--break-system-packages` do môi trường bị PEP 668/không có `python3-venv`.
- Đã xử lý file Word dạng nhị phân OLE (.doc) người dùng gửi và trích xuất được các trường thông tin chính của phiếu xác nhận đặt hàng.
