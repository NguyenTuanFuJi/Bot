## Session: 50b99992-15b2-4bda-ad4c-31823248487c | 2026-04-05 07:38
- Mục tiêu: ## Cái mình có thể “học” từ Dify để nâng BM25 (nhẹ mà hiệu quả) 1) **Pipeline ingestion rõ bước** - Tách 3 bước: nạp dữ liệu → làm sạch/chunk → lập chỉ mục BM25.
- Quyết định:
  - - Mình đang có nền rồi, chỉ cần chuẩn hóa thêm logging theo từng bước.
  - 3) **Hybrid query rewrite (nhẹ)** - Trước khi BM25 search, chuẩn hóa query (bỏ nhiễu, đồng bộ từ đồng nghĩa ngắn).
  - ## 2) Metadata-first retrieval Mỗi ghi chú nên có metadata cơ bản: - nguồn (memory/preferences/session_summaries) - thời gian - loại nội dung (preference, decision, todo, summary) - session id (nếu có) Khi truy xuất: - lọc metadata trước (nếu có điều kiện) - rồi mới rank BM25 ## 3) Query normalizer (nhẹ) Trước khi search: - chuẩn hóa chữ hoa/thường - bỏ khoảng trắng thừa - map một số synonym nghiệp vụ ngắn Ví dụ synonym: - "đăng web" -> "wordpress publish" - "bài fb" -> "facebook post" - "sếp" -> "user preference" ## 4) Observability tối giản Ghi log nhỏ cho mỗi lượt search: - query gốc - query sau normalize - top-k trả về - thời gian xử lý - có được dùng hay bị bỏ qua ## 5) Guardrail context - Giới hạn đoạn trả về theo nhu cầu, không đổ quá nhiều đoạn.
- Việc dang dở:
  - - Mình đang có nền rồi, chỉ cần chuẩn hóa thêm logging theo từng bước.
  - - Không cần vector, vẫn cải thiện recall.
  - ## 2) Metadata-first retrieval Mỗi ghi chú nên có metadata cơ bản: - nguồn (memory/preferences/session_summaries) - thời gian - loại nội dung (preference, decision, todo, summary) - session id (nếu có) Khi truy xuất: - lọc metadata trước (nếu có điều kiện) - rồi mới rank BM25 ## 3) Query normalizer (nhẹ) Trước khi search: - chuẩn hóa chữ hoa/thường - bỏ khoảng trắng thừa - map một số synonym nghiệp vụ ngắn Ví dụ synonym: - "đăng web" -> "wordpress publish" - "bài fb" -> "facebook post" - "sếp" -> "user preference" ## 4) Observability tối giản Ghi log nhỏ cho mỗi lượt search: - query gốc - query sau normalize - top-k trả về - thời gian xử lý - có được dùng hay bị bỏ qua ## 5) Guardrail context - Giới hạn đoạn trả về theo nhu cầu, không đổ quá nhiều đoạn.
- Ràng buộc:
  - 5) **Guardrail cho context** - Giới hạn số đoạn trả về theo ngưỡng token.
  - - Ưu tiên đoạn mới + đoạn đúng metadata mục tiêu.
  - ## 2) Metadata-first retrieval Mỗi ghi chú nên có metadata cơ bản: - nguồn (memory/preferences/session_summaries) - thời gian - loại nội dung (preference, decision, todo, summary) - session id (nếu có) Khi truy xuất: - lọc metadata trước (nếu có điều kiện) - rồi mới rank BM25 ## 3) Query normalizer (nhẹ) Trước khi search: - chuẩn hóa chữ hoa/thường - bỏ khoảng trắng thừa - map một số synonym nghiệp vụ ngắn Ví dụ synonym: - "đăng web" -> "wordpress publish" - "bài fb" -> "facebook post" - "sếp" -> "user preference" ## 4) Observability tối giản Ghi log nhỏ cho mỗi lượt search: - query gốc - query sau normalize - top-k trả về - thời gian xử lý - có được dùng hay bị bỏ qua ## 5) Guardrail context - Giới hạn đoạn trả về theo nhu cầu, không đổ quá nhiều đoạn.
  - - Ưu tiên đoạn mới + đúng loại nội dung.
  - Giới hạn top-k để tránh phình context 5.
- Từ khoá: [dify, memory, https, com, github, tiering, bm25, metadata, skill, msg, context, langgenius]
