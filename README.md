# Bot (workspace backup)

This repository tracks the OpenClaw workspace (non-secret files):
- agent prompts/config in workspace
- memory notes
- scripts

Sensitive data (tokens, API keys, credentials, backups) must NOT be committed.
